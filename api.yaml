# AI Search Platform 連線設定
# 請向系統整合商（SI）確認實際 IP 後修改此檔。
# run.py 與 test.py 啟動時自動讀取，亦可用命令列參數覆蓋。

segment_url: "http://localhost:8001"      # 影像切割服務
encoder_url: "http://localhost:8002"      # 特徵比對服務
timeout: 30                                # HTTP 請求逾時（秒）
